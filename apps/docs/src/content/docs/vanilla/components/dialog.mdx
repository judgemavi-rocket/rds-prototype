---
title: Dialog
description: Displays a Dialog
---

import "@rds/styles/button.css"
import "@rds/styles/dialog.css"
import CodePreview from "/src/components/ui/codePreview.astro"
import { Code, Tabs, TabItem } from "@astrojs/starlight/components";
import ApiReference from "/src/components/ui/apiReference.astro"

<CodePreview code={`
   <dialog id="default-dialog" class='rkt-dialog'>
        <h2 class="rkt-dialog__title">Title</h2>
        <p>This is a sample dialog</p>
        <button id="default-close" class='rkt-button'>Done</button>
   </dialog>
    <script>
        const dialog = document.getElementById('default-dialog');
        const dialogTrigger = document.getElementById('default-dialog-trigger');
        dialogTrigger.addEventListener('click', () => {
            dialog.showModal();
        });
        const dialogClose = document.getElementById('default-close');
        dialogClose.addEventListener('click', () => {
            dialog.close();
        });
    </script>
   `} lang="html">
    <dialog id="default-dialog" class='rkt-dialog'>
        <h2 class="rkt-dialog__title">Title</h2>
        <p>This is a sample dialog</p>
        <button id="default-close" class='rkt-button'>Done</button>
    </dialog>
    <button id="default-dialog-trigger" class='rkt-button'>Open Dialog</button>
    <script>
        {`
            const dialog = document.getElementById('default-dialog');
            const dialogTrigger = document.getElementById('default-dialog-trigger');
            dialogTrigger.addEventListener('click', () => {
                dialog.showModal();
            });
            const dialogClose = document.getElementById('default-close');
            dialogClose.addEventListener('click', () => {
                dialog.close();
            });
        `}
    </script>
</CodePreview>

## Installation

<Tabs>
    <TabItem label="Node">
        <Code code={`npm install @rds/styles/dialog`} frame="terminal" lang="sh" />
    </TabItem>
    <TabItem label="Html">
        <Code code={`<link rel="stylesheet" href="/path-to-import/rds/styles/dialog.css"/>`} lang="html" />
    </TabItem>
</Tabs>



## Examples

### Large

<CodePreview code={`
   <dialog id="large-dialog" class='rkt-dialog' data-size="lg">
        <h2 class="rkt-dialog__title">Title</h2>
        <p>This is a large dialog</p>
        <button class='rkt-button'>Done</button>
   </dialog>
    <script>
        const dialog = document.getElementById('large-dialog');
        const dialogTrigger = document.getElementById('large-dialog-trigger');
        dialogTrigger.addEventListener('click', () => {
            dialog.showModal();
        });
        const largeClose = document.getElementById('large-close');
        largeClose.addEventListener('click', () => {
            dialog.close();
        });
    </script>
   `} lang="html">
    <dialog id="large-dialog" class='rkt-dialog' data-size="lg">
        <h2 class="rkt-dialog__title">Title</h2>
        <p>This is a large dialog</p>
        <button id="large-close" class='rkt-button'>Done</button>
    </dialog>
    <button id="large-dialog-trigger" class='rkt-button'>Open Dialog</button>
    <script>
        {`
            const large = document.getElementById('large-dialog');
            const largeTrigger = document.getElementById('large-dialog-trigger');
            largeTrigger.addEventListener('click', () => {
                large.showModal();
            });
            const largeClose = document.getElementById('large-close');
            largeClose.addEventListener('click', () => {
                large.close();
            });
        `}
    </script>
</CodePreview>

## API Reference
<ApiReference config={[
    {
        prop: 'data-size',
        type: `"lg" | "md"`,
        default: 'md'
    },
]}/>