---
title: Dialog
description: A window overlaid on either the primary windo wor another dialog window, rendering the content underneath inert.
---

import CodePreview from "/src/components/ui/codePreview.astro"
import ApiReference from "/src/components/ui/apiReference.astro"
import { Code } from "@astrojs/starlight/components";

import { Default } from "/src/components/react/dialog/default";
import defaultSrc from "/src/components/react/dialog/default.tsx?raw";

<CodePreview code={defaultSrc} lang="tsx">
    <Default client:load/>
</CodePreview>

## Installation

<Code code={`npm install @rds/react-dialog`} frame="terminal" lang="sh" />

## Examples

### Modal

import { Modal } from "/src/components/react/dialog/modal";
import modalSrc from "/src/components/react/dialog/modal.tsx?raw";

<CodePreview code={modalSrc} lang="tsx">
    <Modal client:load/>
</CodePreview>

### Large

import { Large } from "/src/components/react/dialog/large";
import largeSrc from "/src/components/react/dialog/large.tsx?raw";

<CodePreview code={largeSrc} lang="tsx">
    <Large client:load/>
</CodePreview>

## API Reference

### Root

<ApiReference config={[
    {
        prop: 'modal',
        type: 'boolean',
        default: 'false'
    },
    {
        prop: 'size',
        type: `"lg" | "md"`,
        default: 'md'
    },
    {
        prop: 'open',
        type: 'boolean',
        default: 'false',
        desc: 'Controls the state of dialog'
    },
    {
        prop: 'onOpenChange',
        type: `(open: boolean) => void`,
        desc: 'Event that is emitted everytime dialog state changes.'
    }
]}/>

### Trigger

- Trigger is a button and has same api as native ```<button/>```.
- Provided onClick callback is called prior to opening the dialog. 

### Close

- Close is a button and has same api as native ```<button/>```.
- Provided onClick callback is called prior to closing the dialog. 